# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local secrets
.env

# Generated at runtime, not checked in. These live on the Render persistent
# disk in production -- committing a local copy would fight with real state.
state/dashboard.html
state/trades.jsonl
state/heartbeat.json
