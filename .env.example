HERMES_TRADING_MODE=paper
HERMES_TRADING_I_ACCEPT_RISK=false

# Optional API keys — leave blank for free public data
EXCHANGE_API_KEY=
EXCHANGE_API_SECRET=

# Optional: gates /dashboard and /state/* on the debug server (see
# hermes_trading/debug_server.py). Leave blank to keep those endpoints open.
DEBUG_TOKEN=
